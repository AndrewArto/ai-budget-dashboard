# AI Budget Dashboard — Спецификация

## Обзор

Приложение в строке меню macOS для отслеживания расходов на AI API и оставшегося бюджета по четырём провайдерам: **Anthropic (Claude)**, **OpenAI (GPT)**, **Google (Gemini)** и **xAI (Grok)**. Показывает использование за текущий календарный месяц с визуальной разбивкой по провайдерам.

## Проблема

Ежедневная работа с несколькими AI-моделями без единой картины расходов. У каждого провайдера своя страница биллинга с разным UI, разной частотой обновления и без кросс-провайдерной сводки. Нужен один взгляд: сколько потрачено, сколько осталось, по каждому провайдеру, за месяц.

## Требования

### Основные

- **Иконка в menu bar** — в строке состояния macOS, показывает общий расход или остаток бюджета
- **Выпадающая панель** — клик раскрывает детали по каждому провайдеру:
  - Название провайдера + иконка
  - Потрачено за месяц (USD)
  - Лимит бюджета (настраивается по провайдеру)
  - Остаток бюджета
  - Прогресс-бар (зелёный → жёлтый → красный по мере расхода)
  - Количество токенов (где доступно)
- **Календарный месяц** — сброс 1-го числа каждого месяца автоматически
- **Авто-обновление** — опрос API провайдеров с настраиваемым интервалом (по умолчанию: каждые 15 мин)
- **Ручное обновление** — клик для немедленного обновления
- **Алерты бюджета** — системное уведомление при достижении 80% и 95% бюджета
- **Настройки** — лимиты бюджета по провайдеру, интервал обновления, пороги алертов

### API провайдеров

| Провайдер | Эндпоинт | Авторизация | Примечания |
|-----------|----------|-------------|------------|
| Anthropic | `GET /v1/usage` (Admin API) | Admin API Key | Токены + стоимость по модели |
| OpenAI | `GET /v1/organization/costs` | Admin API Key | Дневная разбивка расходов |
| Google (Gemini) | Cloud Billing API или `generativelanguage` usage | Service Account / API Key | Возможно нужен Cloud Billing export; бесплатный тариф без usage API — фоллбэк на локальный трекинг |
| xAI | `GET /v1/api-key` → заголовки rate limit | API Key | Нет выделенного billing API; трекинг через rate limit заголовки или локальное логирование |

**Стратегия фоллбэка:** Если у провайдера нет usage API (Gemini free tier, xAI), приложение отслеживает использование локально, парся логи OpenClaw. OpenClaw уже логирует все запросы — парсим эти логи.

### Модель данных

```
Provider {
  id: string              // "anthropic" | "openai" | "google" | "xai"
  name: string            // Отображаемое имя
  apiKey: string          // Хранится в macOS Keychain
  monthlyBudget: number   // USD, настраивается пользователем
  currentSpend: number    // USD, получено или рассчитано
  tokensIn: number        // Входящие токены за месяц
  tokensOut: number       // Исходящие токены за месяц
  lastUpdated: Date
}
```

### Отображение в Menu Bar

Компактный формат в строке состояния:

```
$47.23 / $200    ← потрачено / общий бюджет
```

Или режим только иконки с цветной точкой:
- 🟢 до 60%
- 🟡 60-85%
- 🔴 более 85%

### Макет выпадающей панели

```
┌─────────────────────────────┐
│  AI Budget — Февраль 2026   │
│                             │
│  Anthropic     $28.40/$80   │
│  ████████████░░░░  71%      │
│  1.2M in / 380K out         │
│                             │
│  OpenAI        $12.30/$60   │
│  ████████░░░░░░░░  41%      │
│  800K in / 210K out         │
│                             │
│  Google         $4.10/$30   │
│  ████░░░░░░░░░░░░  27%      │
│  2.1M in / 450K out         │
│                             │
│  xAI            $2.43/$30   │
│  ███░░░░░░░░░░░░░  16%      │
│  500K in / 120K out         │
│                             │
│  Итого: $47.23 / $200      │
│  ─────────────────────────  │
│  ↻ Обновлено 2 мин назад   │
│  ⚙ Настройки               │
└─────────────────────────────┘
```

## Технологический стек

| Компонент | Выбор | Обоснование |
|-----------|-------|-------------|
| Фреймворк | **Swift + SwiftUI** | Нативное macOS menu bar приложение, лучшая интеграция с системным UI, Keychain, уведомлениями |
| Menu bar | `MenuBarExtra` (macOS 13+) | Нативный SwiftUI API для menu bar приложений |
| HTTP | `URLSession` | Встроенный, без зависимостей |
| Хранилище | `UserDefaults` + Keychain | Настройки в UserDefaults, API ключи в Keychain |
| Уведомления | `UNUserNotificationCenter` | Нативные уведомления macOS |
| Сборка | Xcode / `swift build` | Стандартный тулчейн |

**Альтернатива (если Swift избыточен):** Electron + menubar npm пакет. Быстрее в разработке, кросс-платформенный, но тяжелее по ресурсам. Не рекомендуется для виджета в строке состояния.

**Альтернатива 2 (легковесная):** Python + rumps. Минимальное menu bar приложение, ~200 строк. Хорошо для MVP.

### Рекомендация: Python + rumps (MVP)

Для скорости разработки и удобства работы агентов:

```
ai-budget-dashboard/
├── main.py              # Точка входа, menu bar приложение (rumps)
├── providers/
│   ├── __init__.py
│   ├── anthropic.py     # Anthropic usage API
│   ├── openai_api.py    # OpenAI billing API
│   ├── google.py        # Google billing / локальный трекинг
│   └── xai.py           # xAI usage / локальный трекинг
├── config.py            # Лимиты бюджета, интервал обновления
├── tracker.py           # Локальный трекинг (фоллбэк)
├── notifier.py          # Уведомления macOS
├── keychain.py          # Интеграция с macOS Keychain
├── requirements.txt     # rumps, requests, keyring
├── setup.py             # py2app для .app бандла
└── assets/
    └── icon.png         # Иконка menu bar (16x16, template image)
```

После работающего MVP → опционально переписать на Swift для нативного ощущения и меньшего потребления ресурсов.

## Конфигурация

Хранится в `~/.config/ai-budget/config.json`:

```json
{
  "providers": {
    "anthropic": { "budget": 80, "enabled": true },
    "openai": { "budget": 60, "enabled": true },
    "google": { "budget": 30, "enabled": true },
    "xai": { "budget": 30, "enabled": true }
  },
  "refreshIntervalMinutes": 15,
  "alertThresholds": [80, 95],
  "displayMode": "compact",
  "localTrackingLogPath": "~/.openclaw/logs/"
}
```

API ключи хранятся в macOS Keychain, не в конфиг-файле.

## Локальный трекинг (фоллбэк)

Для провайдеров без billing API — парсинг логов OpenClaw:

1. Читаем лог-файлы OpenClaw из `~/.openclaw/logs/`
2. Извлекаем: timestamp, провайдер, модель, tokens_in, tokens_out
3. Применяем прайсинг по модели (захардкоженная таблица, обновляется вручную)
4. Агрегируем по месяцу

Таблица цен (пример):
```python
PRICING = {
    "claude-opus-4": {"input": 15.0, "output": 75.0},      # за 1M токенов
    "claude-sonnet-4": {"input": 3.0, "output": 15.0},
    "gpt-4o": {"input": 2.50, "output": 10.0},
    "gemini-2.5-pro": {"input": 1.25, "output": 10.0},
    "grok-3": {"input": 3.0, "output": 15.0},
}
```

## Фазы

### Фаза 1 — MVP (2-3 дня)

- Python + rumps menu bar приложение
- Конфиг-файл с лимитами бюджета
- Anthropic + OpenAI usage API (оба имеют рабочие billing эндпоинты)
- Локальный трекинг как фоллбэк для Google + xAI
- Базовая выпадающая панель с расходом/бюджетом по провайдеру
- Уведомления macOS при превышении порогов

### Фаза 2 — Полировка (1-2 дня)

- Интеграция с Keychain для API ключей
- UI настроек (SwiftUI sheet или простой tkinter)
- Исторический график (тренд за 7/30 дней)
- Упаковка py2app → .app бандл
- Автозапуск при логине (LaunchAgent)

### Фаза 3 — Нативное приложение (опционально)

- Переписать на Swift/SwiftUI если ресурсоёмкость или UX того требуют
- Виджеты для Центра уведомлений macOS

## Не в скоупе

- Нет веб-UI (это локальный десктопный инструмент)
- Нет мультипользовательского / командного трекинга
- Нет генерации счетов
- Нет разбивки стоимости по разговорам (только месячные итоги)

## Открытые вопросы

1. **Доступ к Gemini billing API** — проверить, отдаёт ли Gemini API данные об использовании для API-key авторизации или нужна настройка Cloud Billing
2. **Эндпоинт usage xAI** — проверить, добавил ли xAI billing API (по состоянию на февраль 2026)
3. **Формат логов OpenClaw** — подтвердить структуру логов для локального трекинга
4. **Swift vs Python** — предпочтение: скорость MVP или нативное качество

## Критерии успеха

- Один взгляд на menu bar показывает общий расход на AI
- Разбивка по провайдерам в один клик
- Алерты до исчерпания бюджета
- Стабильная работа на macOS (Apple Silicon)
- Менее 50 МБ RAM, менее 1% CPU
